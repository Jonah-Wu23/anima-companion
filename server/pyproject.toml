[project]
name = "anima-companion-server"
version = "0.1.0"
description = "Backend services for anima companion android"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
