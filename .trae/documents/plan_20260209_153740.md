我将分两步执行：首先验证后端服务可用性，然后根据文档拆解并执行前端开发任务。

### Phase 1: 后端服务验证 (Backend Smoke Test)
1. **启动后端服务**：运行 `scripts/dev/start_server.ps1` 启动 FastAPI 服务 (Port 8000)。
2. **执行冒烟测试**：
   - 创建临时测试脚本 `tests/smoke/api_smoke.py`。
   - 测试 `/v1/chat/text` 接口：发送 "Hello" 确认回复。
   - 测试 `/v1/chat/voice` 接口：发送模拟音频数据确认解析。
   - 验证通过后关闭服务。

### Phase 2: 前端任务拆解与初始化 (Frontend Task Breakdown)
根据 `docs/plans/2026-02-09-p0-frontend-development-spec.md`，我将创建以下任务清单并开始执行：

1. **环境初始化 (Infrastructure)**
   - 安装核心依赖：`zustand` (状态管理), `@tanstack/react-query` (API状态), `@react-three/fiber` (3D), `tailwindcss` (样式)。
   - 初始化 Tailwind CSS 配置与全局样式。
   - 封装 API Client (Axios/Fetch) 并生成/同步 TypeScript 接口定义。

2. **核心 UI 组件开发 (Core UI)**
   - **Shell**: 实现 `ChatShell` 布局，处理移动端适配 (100dvh) 和 SafeArea。
   - **Components**: 开发 `MessagePanel` (消息流), `InputDock` (输入区), `TopBar`。
   - **3D Viewport**: 实现 `Viewport3D` 组件和基础状态机 (Idle/Speaking)。

3. **业务逻辑集成 (Integration)**
   - **Chat Logic**: 对接 `/v1/chat/text`，实现消息发送与流式/非流式渲染。
   - **Voice Logic**: 实现按住说话 (Press-to-talk) 交互，录音并对接 `/v1/chat/voice`。
   - **State Management**: 实现 `sessionStore` 和 `pipelineStore`。

4. **辅助功能 (Features)**
   - 实现设置面板 (`SettingsSheet`) 和数据清除 (`/v1/user/clear`)。
   - 添加基础 A11y 支持 (aria-label, 键盘导航)。

**确认后我将立即执行 Phase 1 和 Phase 2 的任务清单创建。**